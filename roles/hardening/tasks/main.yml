- name: Update apt cache
  apt:
    update_cache: yes

- name: Install packages
  package:
    name:
      - ufw

- community.general.ufw:
    rule: limit
    port: ssh
    proto: tcp

- name: Deny everything and enable UFW
  community.general.ufw:
    state: enabled
    policy: deny