- name: Install packages
  package:
    name:
      - ufw

- community.general.ufw:
    rule: limit
    port: ssh
    proto: tcp

- name: Deny everything and enable UFW
  community.general.ufw:
    state: enabled
    policy: deny